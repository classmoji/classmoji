// Shared with packages/services and packages/tasks, which build the same links.
export {
  gitWeb,
  gitTerms,
  gitContextFor,
  type GitWebContext,
  type ClassroomLike,
  type GitTerms,
} from '@classmoji/utils';
