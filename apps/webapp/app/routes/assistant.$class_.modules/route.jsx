// Re-export from student route (ASSISTANT already included in assertClassroomAccess allowedRoles)
// Navigation paths are role-aware via useLocation()
export { loader, default } from '../student.$class.modules/route';
