// Re-export from the student route: the loader authorises any classroom member,
// so the docked Pages view is identical for assistants.
export { loader, default } from '../student.$class.pages/route';
